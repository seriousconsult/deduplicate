#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "$0")" && pwd)"
PY="$ROOT/.venv/bin/python"
if [[ ! -x "$PY" ]]; then
  echo "Virtualenv not found. Run: python3 install.py" >&2
  exit 1
fi
exec "$PY" "$ROOT/index.py" "$@"
